# GitHub Codespaces Configuration

Configurations specific to GitHub Codespaces development environments.

## Files

- `devcontainer.json` — Development container configuration
- `init.sh` — Bootstrap script for Codespaces
- `extensions.json` — VSCode extensions for Codespaces

## Usage

Place `devcontainer.json` in project root under `.devcontainer/` directory.
