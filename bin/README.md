# Custom Executables

Custom shell scripts, utilities, and tools.

## Usage

Add this directory to your PATH to use scripts from anywhere:

```bash
export PATH="$HOME/.dotfiles/bin:$PATH"
```

Or symlink individual scripts to `~/bin/` or `/usr/local/bin/`.
