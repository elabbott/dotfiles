# macOS Configuration

macOS-specific settings, preferences, and utilities.

## Files

- `defaults.sh` — macOS system defaults (Finder, Dock, etc.)
- `homebrew.txt` — Homebrew packages to install
- `preferences.sh` — Application preferences

## Usage

Run `defaults.sh` to apply macOS settings (requires restart in some cases).
