# Installation and Utility Scripts

Scripts for installing, linking, and maintaining dotfiles.

## Files

- `install.sh` — Main installation script
- `detect-os.sh` — OS detection utility
- `link-dotfiles.sh` — Symlink dotfiles to home directory
- `update.sh` — Update dotfiles from repository

## Usage

Run `install.sh` to bootstrap your environment.
