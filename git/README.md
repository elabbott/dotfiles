# Git Configuration

Git configuration files and hooks.

## Files

- `config` — Git global configuration (user config, aliases, settings)
- `ignore` — Global gitignore rules
- `hooks/` — Git hooks (pre-commit, post-checkout, etc.)

## Usage

Link or copy `config` to `~/.config/git/config` or `~/.gitconfig`.
